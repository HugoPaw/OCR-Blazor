@code {
    Dictionary<string, bool> toggles = new()
{
    { "Texterkennung", false },
    { "AITools", false },
    { "Projekte", false },
    { "Sport", false },
    { "Lernen", false },
    { "Sicherheit", false }, 
    { "Spiele", false },
    { "Nasa", false }
};


    void Toggle(string section) => toggles[section] = !toggles[section];
}

<nav class="sidebar-nav">

    <NavLink class="nav-link main" href="/">🏠 Home</NavLink>

    <!-- Texterkennung -->
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Texterkennung")'>📝 Texterkennung</button>
        @if (toggles["Texterkennung"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/OCR">OCR</NavLink>
                <NavLink class="nav-link" href="/PdfTextExtractor">PDF Extractor</NavLink>
                <NavLink class="nav-link" href="/ImageTextExtractor">Bild Extractor</NavLink>
            </div>
        }
    </div>

    <!-- AI Tools -->
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("AITools")'>🤖 AI Tools</button>
        @if (toggles["AITools"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/sprachen">Sprachen erkennen</NavLink>
                <NavLink class="nav-link" href="/codecleaner">Code Cleaner</NavLink>
                <NavLink class="nav-link" href="/song">Song to MP3</NavLink>
            </div>
        }
    </div>

    <!-- Projekte -->
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Projekte")'>💼 Projekte & Demos</button>
        @if (toggles["Projekte"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/wetter">Wetter</NavLink>
                <NavLink class="nav-link" href="/lebenslauf">Lebenslauf</NavLink>
                <NavLink class="nav-link" href="/cards">Card View</NavLink>
                <NavLink class="nav-link" href="/ball">3D Ball</NavLink>
            </div>
        }
    </div>

    <!-- Sport -->
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Sport")'>⚽ Sport</button>
        @if (toggles["Sport"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/fusball">Fussball</NavLink>
                <NavLink class="nav-link" href="/swissteams">Swiss Teams</NavLink>
            </div>
        }
    </div>

    <!-- Lernen -->
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Lernen")'>📘 Lernen</button>
        @if (toggles["Lernen"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/grundlagen">Grundlagen</NavLink>
            </div>
        }
    </div>
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Sicherheit")'>🔐 Sicherheit & Datenschutz</button>
        @if (toggles["Sicherheit"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/passwort">Passwort Generator</NavLink>
                <NavLink class="nav-link" href="/qrcode">Zwei-Faktor-Login</NavLink>
                <NavLink class="nav-link" href="/encrypt">Datenverschlüsselung</NavLink>
            </div>
        }
    </div>
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Spiele")'>Spiele</button>
        @if (toggles["Spiele"])
        {
            <div class="nav-sub">
                <NavLink class="nav-link" href="/BJ">Black Jack</NavLink>
                <NavLink class="nav-link" href="/Hangman">Hangman</NavLink>
               
            </div>
        }
    </div>
    <div class="nav-group">
        <button class="nav-header" @onclick='() => Toggle("Nasa")'>Nasa</button>
        @if (toggles["Nasa"])
        {
            <div class="nav-sub">
                
                <NavLink class="nav-link" href="/Nasa">Nasa</NavLink>
                <NavLink class="nav-link" href="/NeoWs">neoWs</NavLink>
                <NavLink class="nav-link" href="/Marsrover">Mars Rover</NavLink>
            </div>
        }
    </div>
</nav>
